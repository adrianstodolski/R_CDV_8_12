# Wektor "imiona"
imiona <- c("Tomek", "Kasia", "Ola")
# Wektor "wiek"
wiek <- c(30,32,37)
# Ramka danych "osoby" zawierająca wektor "imiona" oraz "wiek"
osoby <- data.frame(imiona, wiek)
# Średnia wieku zapisana do zmiennej
average <- mean(wiek)
